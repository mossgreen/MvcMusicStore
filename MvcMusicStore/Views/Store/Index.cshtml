@model IEnumerable<MvcMusicStore.Models.Genre>
@{
    ViewBag.Title = "Index";
}

<h2>Browse Genres</h2>
<p>
    Select from @Model.Count() genress:
</p>

<ul>
@foreach (var genre in Model)
{
    <li>@genre.Name</li>
}

</ul>

